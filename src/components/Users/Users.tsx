import * as React from "react";
import List from "./List";

interface IAppProps {}

const Users: React.FunctionComponent<IAppProps> = (props) => {
  return (
      <List />
  );
};

export default Users;

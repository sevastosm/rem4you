import * as React from "react";
import List from "./List";

interface IAppProps {}

const Members: React.FunctionComponent<IAppProps> = (props) => {
  return (
      <List />
  );
};

export default Members;
